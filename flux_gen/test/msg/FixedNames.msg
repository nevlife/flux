# Fixed-length arrays whose length still rides in the frame: the string array and the jagged
# element array both carry a descriptor, so a frame can disagree with the schema's N.
string[2] names
Tensor[2] pair
