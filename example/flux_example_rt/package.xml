<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>flux_example_rt</name>
  <version>0.0.0</version>
  <description>
    Thread scheduling for a chain that spans processes. rt_preflight asks whether this host can
    hold a schedule before anything is applied. rt_chain reads its own stage out of the chain
    declaration file and hands it to the executor that owns the thread. C++ only: the Python path
    is not an RT target.
  </description>
  <maintainer email="nevlife000720@gmail.com">nevlife</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>flux_cpp</depend>
  <depend>flux_example_msgs</depend>
  <depend>rclcpp</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
