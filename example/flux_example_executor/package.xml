<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>flux_example_executor</name>
  <version>0.0.0</version>
  <description>
    Driving flux subscriptions. merged_sub waits on a flux channel and a ROS subscription in one
    io_uring, so both transports reach one thread with one syscall. partitioned_sub gives each
    callback group its own thread, so a slow callback delays nobody else.
  </description>
  <maintainer email="nevlife000720@gmail.com">nevlife</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>flux_cpp</depend>
  <depend>flux_example_msgs</depend>
  <depend>rclcpp</depend>
  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>std_srvs</depend>

  <exec_depend>flux_py</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
