<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>flux_example_gpu</name>
  <version>0.0.0</version>
  <description>
    Frames a kernel writes and reads without a staging buffer. Declaring Device::Cuda makes flux
    create the stream both seams fence on; the wire is unchanged, so a host subscriber on the same
    topic keeps working. Reports what the release fence costs.
  </description>
  <maintainer email="nevlife000720@gmail.com">nevlife</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>flux_cpp</depend>
  <depend>rclcpp</depend>
  <depend>rclpy</depend>

  <exec_depend>flux_py</exec_depend>
  <exec_depend>python3-numpy</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
