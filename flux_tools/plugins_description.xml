<library path="flux_tools_rviz">
  <class
    name="flux_tools/Image"
    type="flux_tools::rviz::ImageDisplay"
    base_class_type="rviz_common::Display"
  >
    <description>
      Shows a sensor_msgs/Image published over a flux channel, read from shared memory inside
      rviz2 without going through DDS.
    </description>
  </class>
  <class
    name="flux_tools/PointCloud2"
    type="flux_tools::rviz::PointCloud2Display"
    base_class_type="rviz_common::Display"
  >
    <description>
      Shows a sensor_msgs/PointCloud2 published over a flux channel, read from shared memory
      inside rviz2 without going through DDS. Rendering is the stock point cloud renderer.
    </description>
  </class>
  <class
    name="flux_tools/DepthCloud"
    type="flux_tools::rviz::DepthCloudDisplay"
    base_class_type="rviz_common::Display"
  >
    <description>
      Back-projects a depth sensor_msgs/Image published over a flux channel into 3D points,
      using the CameraInfo published next to it on DDS. Rendering is the stock point cloud
      renderer.
    </description>
  </class>
</library>
