# The message docs/api.md section 2 generates its adapter from. Generated at build time under
# the package name my_pkg, so the document's my_pkg::flux_msg::Cloud is the type that compiles.
float32[] x
uint32 width
string label
